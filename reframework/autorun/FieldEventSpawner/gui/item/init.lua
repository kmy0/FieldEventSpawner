local this = require("FieldEventSpawner.gui.item.items")
this.values = require("FieldEventSpawner.gui.item.values")

---@param state GuiDataState
function this.init(state)
    this._init(state)
end

return this
